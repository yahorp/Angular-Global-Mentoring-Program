export interface User {
    id: string;
    firstName: string;
    lastName: string;
}

export interface Course {
    id: number;
    title: string;
    creationDate: string;
    duration: number;
    description: string;
}